import board
import time
from neopixel import NeoPixel
import random

np = NeoPixel(board.D2, 31, auto_write=False, brightness = 0.2)

magenta = (255,0,255)
white = (255,255,255)
orange = (255, 135, 30)
lightorange = (200, 34, 0)
palet_list = [orange, magenta, white, lightorange]
anything = random.choice(palet_list)

sparkleween = 5

def larson(bground, movinglight, num, speed):
    pixel = 0
    direction = 1
    count 0
    while count < num:
        np.fill(bground)
        np.show()
        np[pixel
        

while True:
    larson(orange)
